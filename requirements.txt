# UZAYTEK HRMA Dependencies
# Core web framework
flask>=3.0.0
flask-cors>=4.0.0

# Scientific computing
# numpy<2 pini ZORUNLU: anaconda tabanlı kurulumlarda numpy 2.x, pandas/sklearn
# binary uyumunu kırıyor (2026-07-12 kurulum kazası — dtype size mismatch)
numpy>=1.24.0,<2
scipy>=1.11.0

# Cantera — denge termokimyası. ZORUNLUDUR, opsiyonel değildir.
#
# v2.6.2'de tespit edildi: cantera bu dosyada YOKTU, dolayısıyla temiz bir
# kurulumda import başarısız oluyor ve CombustionAnalyzer sessizce
# `_fallback_equilibrium_composition`'a düşüyordu. O yedek, ürün bileşimini
# İTİCİ ÇİFTİNDEN BAĞIMSIZ sabit bir sözlükten veriyor: LOX/HTPB gibi hiç azot
# içermeyen bir çiftte bile ürünlerin %54'ü N2 sayılıyor ve ortalama molekül
# ağırlığı her çiftte 29,6 g/mol'e çakılıyordu. Ölçülen c* hatası %4,4 ile
# %13,4 arası — ve kullanıcıya hiçbir uyarı verilmiyordu.
#
# Yol kullanıcıya görünür: /calculate hibrit yanma analizi panelini bu
# sınıftan besler (hrma/app.py:723).
#
# numpy uyumu: cantera 3.1.0 `numpy<3,>=1.12` deklare eder, yukarıdaki
# numpy<2 pinimizle çakışmaz (build123d'nin aksine — o yüzden o opsiyonel).
cantera>=3.0.0,<4

# Visualization
plotly>=5.18.0
matplotlib>=3.7.0

# Data analysis
pandas>=2.0.0
openpyxl>=3.1.0        # /api/export-xlsx insan-dostu Excel dışa aktarma (v2.5.2)

# 3D CAD and geometry
shapely>=2.0.0         # star grain gerçek yanma-yüzeyi modeli (poligon ofseti)
trimesh>=3.20.0
manifold3d>=2.0.0      # trimesh boolean backend (enjektör orifis delikleri)
# build123d (gerçek STEP export, OCC tabanlı ~500 MB) OPSİYONELDİR ve bu
# dosyadan BİLEREK çıkarılmıştır: 0.11.x metadata'sı numpy>=2 istiyor, bizim
# numpy<2 pinimizle çakışıyor ve `pip install -r requirements.txt` TEMİZ bir
# makinede tümüyle çözümsüz kalıyordu (CI ilk koşusunda yakalandı,
# 2026-07-23). Kod build123d yokluğunu zaten açıkça ele alıyor: STEP export
# istendiğinde kullanıcıya net mesaj döner, sessizce düşmez
# (hrma/app.py:2490), testler de pytest.importorskip ile atlar.
# STEP export isteyenler ayrıca kurar:
#     pip install build123d
# (numpy 2.x'e yükseltmek istemiyorsanız conda ortamı önerilir; numpy<2 pini
#  2026-07-12 kurulum kazasının sonucudur, yukarıdaki nota bakınız.)
ezdxf>=1.1.0           # DXF imalat çizimleri

# Static chart export (teknik çizim PDF'leri)
# 0.2.1 pini: kaleido 1.x, plotly<6.1.1 ile write_image üretemiyor
kaleido==0.2.1
reportlab>=4.0.0       # PDF rapor/çizim exportları (/api/export-pdf, /api/export-drawings-pdf)

# Image processing
Pillow>=10.0.0

# Web scraping and HTTP requests (for live propellant data)
requests>=2.31.0
beautifulsoup4>=4.12.0
aiohttp>=3.9.0

# XML processing
lxml>=4.9.0

# NASA CEA calculations (optional - for enhanced accuracy)
rocketcea>=1.2.0

# Thermodynamic properties (NIST RefProp tabanlı)
CoolProp>=6.5.0

# Web servers
gunicorn>=21.2.0;platform_system!="Windows"
waitress>=2.1.0;platform_system=="Windows"

# Build tools (for creating executables)
pyinstaller>=5.13.0

# Development tools (optional)
black>=23.0.0
flake8>=6.0.0